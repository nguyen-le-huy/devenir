name: Docker Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify Environment Files
        run: |
          # Check critical .env files exist
          if [ ! -f server/.env ]; then
            echo "❌ server/.env not found. Please create it!"
            exit 1
          fi
          echo "✅ Environment files verified"

      - name: Build Docker Images
        run: |
          echo "🐳 Building Docker images..."
          docker compose build
          echo "✅ Images built successfully!"

      - name: Deploy Containers
        run: |
          echo "🔄 Restarting containers..."
          docker compose up -d
          echo "✅ Containers restarted!"

      - name: Health Check
        run: |
          echo "⏳ Waiting for services to start..."
          sleep 20
          
          echo "📊 Container Status:"
          docker compose ps
          
          # Test local endpoints
          echo ""
          echo "🧪 Testing services..."
          curl -f http://localhost:5173 > /dev/null && echo "✅ Client OK" || echo "⚠️  Client not ready"
          curl -f http://localhost:5174 > /dev/null && echo "✅ Admin OK" || echo "⚠️  Admin not ready"
          curl -f http://localhost:3111 > /dev/null && echo "✅ API OK" || echo "⚠️  API not ready"

      - name: Reload Nginx
        run: |
          echo "🔄 Reloading Nginx..."
          sudo nginx -t && sudo nginx -s reload || echo "⚠️  Nginx reload skipped"
          echo "✅ Nginx reloaded"

      - name: Cleanup
        run: |
          echo "🧹 Cleaning up dangling images..."
          docker image prune -f
          echo "✅ Cleanup completed!"

  notify:
    runs-on: self-hosted
    needs: deploy
    if: always()
    steps:
      - name: Deployment Status
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "✅ DEPLOY SUCCESSFUL!"
            echo ""
            echo "🌐 Public URLs:"
            echo "   Client: https://hystudio-server.tail86e288.ts.net/"
            echo "   Admin:  https://hystudio-server.tail86e288.ts.net/admin/"
            echo "   API:    https://hystudio-server.tail86e288.ts.net/api"
            echo "   N8N:    https://hystudio-server.tail86e288.ts.net/n8n/"
          else
            echo "❌ DEPLOY FAILED!"
            echo "📋 Check logs: docker compose logs -f"
          fi